{
  "name": "hovertab",
  "version": "0.0.1",
  "type": "module",
  "description": "Chrome extension that changes cursor for links that open in new tabs",
  "private": true,
  "scripts": {
    "build": "npm run clean && vite build",
    "clean": "rm -rf dist",
    "copy": "mkdir -p dist && cp -r app/* dist/",
    "zip": "cd dist && zip -r ../hovertab.zip .",
    "dist": "npm run build && cd dist && zip -r ../hovertab.zip . && cd ..",
    "package": "npm run build && npm run zip"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.3",
    "vite": "^5.0.0",
    "vite-plugin-static-copy": "^3.1.1"
  }
}
